<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
      http://www.liquibase.org/xml/ns/dbchangelog
      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <property name="schema_name" value="sert_ingestor"/>

    <changeSet id="20260703-anag-pa-emittente-codice-index" author="system">
        <preConditions onFail="MARK_RAN" onError="HALT">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*)
                FROM pg_indexes
                WHERE schemaname = '${schema_name}'
                  AND tablename = 'anag_pa_emittente'
                  AND indexdef ILIKE '%(codice)%'
            </sqlCheck>
        </preConditions>

        <createIndex schemaName="${schema_name}"
                     tableName="ANAG_PA_EMITTENTE"
                     indexName="IDX_ANAG_PA_EMITTENTE_CODICE">
            <column name="CODICE"/>
        </createIndex>

        <rollback>
            <dropIndex schemaName="${schema_name}"
                       tableName="ANAG_PA_EMITTENTE"
                       indexName="IDX_ANAG_PA_EMITTENTE_CODICE"/>
        </rollback>
    </changeSet>
</databaseChangeLog>
