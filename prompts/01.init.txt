Usa le regole definite in .github/copilot-instructions.md. Inizia ora con questo prompt:

Il progetto esistente usa Spring Boot 3 + Java 17, Spring Batch e Quartz.
Esistono già BatchConfiguration e QuartzConfiguration e liquibase per BATCH_ e QRTZ_.
Voglio creare le fondamenta comuni dell’ingestor, coerenti con questo progetto.

Crea nel package it.pagopa.cruscotto.ingestion.ingestor (o simile):

1) enum EntityName { POSITION, POSITION_TOKENS, POSITION_TRANSFERS, EXTRA_INFO, EVENTS_WF }

2) record RunContext(
   String runId,
   EntityName entity,
   Instant runStart
)
- factory static RunContext.of(EntityName entity, String runId) che usa runStart=Instant.now()
- runId è ricevuto da Quartz/Batch JobParameters (non generarlo qui)

3) class IngestionConfig con proprietà configurabili (con @ConfigurationProperties(prefix="ingestion")):
   - Duration initialWindow = 5m
   - int maxWindowHalvingAttempts = 10
   - int bulkInsertSize = 5000
   - GuardrailsConfig guardrails:
       enableMaxDuration=true, maxDuration=50m
       enableMaxQueries=false, maxQueries=120
       enableMaxRows=false, maxRows=2_000_000

4) LogHelper:
   metodi statici info/warn/error che richiedono RunContext e una phase string:
   - phase: START, END, WINDOW, CHECKPOINT, NOOP, ERROR
   - ogni log deve includere runId e entity

Output: solo codice Java, compilabile.
Non implementare ancora ADX, DB, scheduler, job batch.
